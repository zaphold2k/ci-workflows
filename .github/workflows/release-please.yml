name: Release please

on:
  push:
    branches: ["main"]

permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    uses: ./.github/workflows/release.yml
    with:
      target_branch: main
    secrets:
      release_token: ${{ secrets.RELEASE_PLEASE_TOKEN }}
