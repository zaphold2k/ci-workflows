{
  "name": "ci-workflows",
  "version": "0.0.0",
  "private": true,
  "description": "Reusable CI, quality-ratchet, and multi-arch Docker publishing workflows for GitHub Actions",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "node --test",
    "lint": "eslint scripts",
    "format": "prettier --check .",
    "format:write": "prettier --write ."
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.11.0",
    "js-yaml": "^5.4.2",
    "prettier": "^3.9.8"
  }
}
